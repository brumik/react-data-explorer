describe('ChartEditor/schemaGenerator', () => {
    xtest('should return correct schema for stacked chart', () => { })
    xtest('should return correct schema for pie chart', () => { })
    xtest('should return correct schema for grouped bar chart', () => { })
});
describe('ChartEditor/schemaToDefaultOptions', () => {
    xtest('should get the correct default options from pie chart', () => { })
    xtest('should get the correct default options from stacked chart', () => { })
    xtest('should get the correct default options from groupd bar chart', () => { })
});
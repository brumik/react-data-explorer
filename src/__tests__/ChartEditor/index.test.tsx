describe('ChartEditor', () => {
    xtest('should render the form closed', () => { })
    xtest('should render the form opened', () => { })
    xtest('should apply the changes in form to chart', () => { })
});